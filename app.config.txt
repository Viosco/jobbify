import 'dotenv/config';

export default {

  "expo": {
    "scheme": "acme",
    "web": {
      "bundler": "metro"
    },
    "name": "jobbify",
    "slug": "jobbify"
  },
  extra: {
    API_KEY: process.env.RAPID_API_KEY,
  }
}
